name: Update FITri Calendar

on:
  schedule:
    - cron: '0 0 * * 1' # Ogni lunedì a mezzanotte
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0 # Recupera tutta la storia per evitare conflitti

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Playwright
      run: |
        python -m pip install --upgrade pip
        pip install playwright
        npx playwright install --with-deps chromium

    - name: Run diagnostic scraper
      run: python scrape_fitri.py

    - name: Commit and push changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "Aggiornamento automatico calendario gare [skip ci]"
        file_pattern: 'gare_fitri_2026.txt'
        commit_user_name: GitHub Action
        commit_user_email: action@github.com
